there is very good library named "NextAuth" 

to install nextauth library..
"npm install next-auth"

Note- "api/auth/[...nextauth]/route.js ", it is standard path for NextAuth library  , it is very important , nothing can replace it 
