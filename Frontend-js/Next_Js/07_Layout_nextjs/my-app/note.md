when we want access logout page we have to follow "http://localhost:3000/admin/logout" this route generally if file structure is " admin->logout->page.js"

but if we want access same logout without admin in route like this "http://localhost:3000/logout" then file structure woul be same but we wrapped the admin parrenthesis() like this 
(admin)->login->page.js